import { ScreenPlaceholder } from "@/components/screen-placeholder";

export default function Page() {
  return <ScreenPlaceholder title="Widget gallery" tag="Supporting surface" note="Live catalog with sample-data + resolved-config previews and grab-embed — built in Phase 3 (specs/10-link-cms/06-widget-gallery.md)." />;
}
